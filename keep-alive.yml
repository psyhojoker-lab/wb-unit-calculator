name: Keep Render Alive

on:
  schedule:
    - cron: "*/14 * * * *"  # Каждые 14 минут
  workflow_dispatch:        # Позволяет запускать вручную

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: |
          curl -s https://wb-unit-calculator.onrender.com/health
